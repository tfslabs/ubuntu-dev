FROM ubuntu:noble

WORKDIR /build

COPY --chown=root:root pm_installs/apt.sh /build/apt.sh
COPY --chown=root:root pm_installs/conda.sh /build/conda.sh

RUN chmod +x /build/*.sh
RUN ["/bin/bash", "/build/apt_rpi.sh"]
RUN ["/bin/bash", "/build/conda_rpi.sh"]

CMD ["/bin/bash"]
