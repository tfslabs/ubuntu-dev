FROM ubuntu:noble

WORKDIR /build

COPY --chown=root:root pm_installs/apt_rpi.sh /build/apt_rpi.sh
COPY --chown=root:root pm_installs/conda_rpi.sh /build/conda_rpi.sh

RUN chmod +x /build/*.sh
RUN ["/bin/bash", "/build/apt_rpi.sh"]
RUN ["/bin/bash", "/build/conda_rpi.sh"]

CMD ["/bin/bash"]
